### Image segmentation in Android with Tensorflow | Android + Tensorflow | Kotlin

#
This app is integrated with ML image segmentation model and a tensorflow library to detect the object and segment the image.